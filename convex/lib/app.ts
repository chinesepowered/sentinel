/** Per-app identity. The only file that differs between our hackathon repos. */
export const APP_SLUG = "sentinel";
/** Prefix for per-case email routing codes, e.g. SEN-7F3K. */
export const CASE_PREFIX = "SEN";
export const APP_NAME = "Sentinel";
/** The seeded family everyone lands on, so the app is never empty for a judge. */
export const DEMO_SLUG = "demo-family";
/** Risk at or above which the guardians get an email, not just a dashboard row. */
export const ALERT_THRESHOLD = 60;
